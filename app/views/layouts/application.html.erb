<!DOCTYPE html>
<html>
<head>
  <title>I Lost Mah Pencil</title>
  <%= stylesheet_link_tag    "application" %>
  <%= stylesheet_link_tag    "main" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class = "content center">
    <% flash.each do |k, v|%>
       <div class = "flash">
	 <%= value %>
       </div>
    <% end %>

    <% if signed_in? %> 
       <%= link_to "Sign out", signout_path, :method => :delete %>
    <% else %>
       <%= link_to "Sign in", signin_path %> or <%= link_to "Sign
       up!", signup_path %>
    <% end %>
    <%= yield %>

    <%= debug(params) if Rails.env.development? %>
  </div>
</body>
</html>
